import React, { Component } from 'react';
import HomeView from './home-view';

class Home extends Component {
    render() {
        return (
            <HomeView name="PEAM" />
        );
    }
}

export default Home;
