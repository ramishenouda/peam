import React from 'react';

import { ProjectRequirement as View } from './requirement-view';

type Props = {
    showAdd?: boolean;
};

export const ProjectRequirements = (props: Props) => {
    return (
        <View />
    );
};
