import * as React from 'react';

import { Container } from './settings-style';

type Props = {
    
};
export const SettingsView = (props: Props) => {
    return (
        <Container>
            Settings
        </Container>
    );
};