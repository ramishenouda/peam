import styled from 'styled-components';

export const Accept = styled.div`
    cursor: pointer;
    :hover {
        background: #000;
    }
`

export const Reject = styled.div`
    cursor: pointer;
    :hover {
        background: #ddd;
    }
`