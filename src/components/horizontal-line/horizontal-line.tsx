import React from 'react';
type Props = {
    width: string,
    color: string,
    textAlign: 'center' | string
};

export const HorizontalLine = (props: Props) => {
    return (
        ''
    );
};